# Vercel + Supabase integration (auto-synced by Vercel Marketplace)
# https://vercel.com/marketplace/supabase
#
# Production already receives:
#   SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY
#   NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY
#   POSTGRES_URL, POSTGRES_URL_NON_POOLING, etc.
#
# Vite reads these at build time via vite.config.ts (no VITE_ prefix required).
#
# Optional overrides:
# APP_URL=https://thehub-6426.vercel.app
# VITE_APP_URL=https://thehub-6426.vercel.app
#
# Sign-in emails (bypasses Supabase built-in ~3 emails/hour limit when set):
# RESEND_API_KEY=re_...
# SIGN_IN_EMAIL_FROM=Pratus <auth@yourdomain.com>
#
# Apply database schema (after vercel env pull):
#   npx vercel env pull .env.vercel.production --environment=production
#   npm run db:apply
#
# Integration / E2E tests (optional — see TESTING.md):
#   SUPABASE_URL=...
#   SUPABASE_SERVICE_ROLE_KEY=...
#   SUPABASE_ANON_KEY=...
#   TEST_USER_EMAIL=test-user@example.com
#   TEST_USER_PASSWORD=...
#   PLAYWRIGHT_BASE_URL=https://thehub-6426.vercel.app
#
# Applies only migrations not yet recorded in public.schema_migrations.
# Existing databases are baselined on first run (no SQL re-run).
